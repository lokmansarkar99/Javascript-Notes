// Template Literals
// Allows embedding variables & expressions in strings using backticks (`) instead of quotes.

const name = "Alice";
const age = 25;
console.log(`My name is ${name} and I am ${age} years old.`);

// Multi-line string
const message = `This is
a multi-line
string.`;
console.log(message);
